query GetCollections {
  collections(first: 8) {
    edges {
      node {
        handle
        id
        title
        image {
          id
          altText
          src
        }
      }
    }
  }
}
